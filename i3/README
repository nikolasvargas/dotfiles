-------------
i3 i3blocks i3status i3lock xorg     # i3 base
xfce4-terminal curl                  # basic terminal setup
sxiv feh                             # image viewer & background display control
xfce4-notifyd                        # xfce4 notification daemon
dunst                                # lightweight notification daemon
arandr                               # monitor/display UI settings
network-manager                      # network service manager
network-manager-applet               # network applet
xfce4-powermanager acpi              # for laptop power management
pulseaudio pulseaudio-utils          # audio support
pavucontrol                          # audio control
scrot imagemagick xautolock          # screen lock
thunar or nautilus                   # file manager
zsh neovim git bat geany             # developer tools
firefox                              # browser
gdebi htop                           # operations tools

login display manager:
---------------------------------------
sudo apt install slim (does not need any dependency)

Additionally:
-------------
enable screen lock script:
-------------------------
chmod +x ~/.config/i3/scripts/lock

# easy run ubuntu
sudo apt install i3 i3blocks i3lock xorg \
    xfce4-terminal curl \
    sxiv feh \
    xfce4-notifyd \
    arandr \
    network-manager \
    xfce4-powermanager acpi \
    pulseaudio pulseaudio-utils \
    pavucontrol \
    scrot imagemagick xautolock \
    thunar or nautilus \
    zsh neovim git bat geany \
    firefox \
    gdebi htop

# easy run fedora
sudo dnf install i3 i3blocks i3status i3lock \
    alsa-lib alsa-utils \
    sxiv feh ImageMagick \
    arandr \
    dunst \
    xclip \
    scrot xautolock \
    light brightnessctl \
    network-manager-applet \
    git-cola \
    ripgrep fd-find
